import { CarouselContentLeftPipe } from './carousel-content-left.pipe';

describe('CarouselContentLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new CarouselContentLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
