import { CarouselContentWidthPipe } from './carousel-content-width.pipe';

describe('CarouselContentWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new CarouselContentWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
