import { CarouselPositionTransformPipe } from './carousel-position-transform.pipe';

describe('CarouselPositionTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CarouselPositionTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
