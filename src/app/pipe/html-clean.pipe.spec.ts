import { HtmlCleanPipe } from './html-clean.pipe';

describe('HtmlCleanPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlCleanPipe();
    expect(pipe).toBeTruthy();
  });
});
